The valuation demo pipeline reads `datasets/seed_prices.csv` and `datasets/seed_transactions.csv`.

Those files are tiny seed data for implementation checks. They are not a representative market dataset.
